# This will be instructions for this specific codebase

### rule#1:
- statement: I do not have code example. But me myself I like "orchestrator classes" if there is a lot of logic going on. So "orchestrator design pattern"
- why: There are plenty of "over the place code" where one methos is around 1.5k lines of code doing every check and doing everything - so let's try to keep best practices and easy readability and if possible best maintanability
